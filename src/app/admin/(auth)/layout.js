/* =============================================
   🏗️ AUTH LAYOUT — Untuk halaman login
   
   Layout minimal tanpa sidebar & tanpa auth check.
   ============================================= */

export const metadata = {
  title: "Login Admin | Magic Pencil",
};

export default function AuthLayout({ children }) {
  return <>{children}</>;
}
