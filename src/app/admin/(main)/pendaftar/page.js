/* =============================================
   👥 PENDAFTAR PAGE — Data Pendaftar
   
   Tabel pendaftar dengan filter, search,
   update status, dan hapus.
   ============================================= */

import PendaftarTable from "@/components/PendaftarTable";

export default function PendaftarPage() {
  return <PendaftarTable />;
}
