/* =============================================
   👦 HALAMAN MURID — Data Seluruh Peserta
   
   Data diambil dari tabel pendaftar.
   Ditampilkan dalam bentuk kartu profil.
   ============================================= */

import MuridCards from "@/components/MuridCards";

export default function MuridPage() {
  return <MuridCards />;
}
